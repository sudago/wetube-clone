extends ../base

block content 
  h4 Update Video
  //- method의 기본값은 GET임. GET은 검색하면 주소창에 나옴. 
  //- POST는 파일을 보내거나 DB값을 바꿀것을 보낼 때 사용. ex) 로그인
  form(method="POST") 
    input(name="title", placeholder="Video Title", value=video.title, required)
    input(placeholder="Description", required, type="text", 
      name="description", minlength=2, value=video.description)
    input(placeholder="Hashtags, separated by comma.", required, type="text", 
      name="hashtags", value=video.hashtags.join())
      //- replace(/#/gi, '') 하면 # 표시가 뜨지않게 됨.
    input(value="Save", type="submit")