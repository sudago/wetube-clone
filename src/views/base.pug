doctype html 
html(lang="ko")
  head
    title #{pageTitle} | Wetube
    link(rel="stylesheet" href="http://unpkg.com/mvp.css")
  body
    header
      //- 다른 text 없이 변수 하나만 갖는 경우에는 아래와 같이 =를 쓴다.
      h1=pageTitle
      nav 
        ul 
          li 
            a(href="/videos/upload") Upload Video
          li
            a(href="/") Home
          li 
            a(href="/search") Search
          li 
            a(href="/join") Join
    main
      block content
  include partials/footer